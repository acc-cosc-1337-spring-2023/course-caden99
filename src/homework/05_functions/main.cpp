/*
 If my main is an infinite loop please kill the terminal and try again. I'm not sure if this is a bug or something but sometimes it repeats itself, other times it's normal.
*/

#include "func.h"
int main() 
{
	auto num = 5
	run_menu();
	return 0;
}